DATA SEGMENT
LED DB 3FH,06H,5BH,4FH,66H,6DH,7DH,07H,7FH,6FH,79H
DATA ENDS
STACK SEGMENT stack
	DATA2 DW 20 DUP(?)
STACK ENDS
CODE SEGMENT
ASSUME CS:CODE,DS:DATA,SS:STACK
START:
	  MOV AX,DATA
	  MOV DS,AX
	  MOV AX,STACK
	  MOV SS,AX
	  MOV AL,10000010B
	  MOV DX,0606H
	  OUT DX,AL      ;¿ØÖÆ×Ö
	  
NEXT: MOV DX,0602H
	  IN AL,DX ;¿ª¹Øin
	  PUSH AX
	  MOV DL,2
	  MOV SI,0
	  
NEXT4:MUL DL
	  CMP AL,80H
	  JA  NEXT2
	  JE  NEXT5
	  JB  NEXT4 
NEXT5:POP AX	  
	  
NEXT1:
	  INC SI
	  MOV AH,0
	  DIV DL
	  CMP AL,0
	  JA NEXT1  
	  
	  MOV AX,SI
	  MOV BX,OFFSET LED
	  XLAT
	  JMP NEXT3

NEXT2:
	  MOV BX,OFFSET LED
	  MOV AL,0AH
	  XLAT
	
NEXT3:MOV DX,0600H
	  OUT DX,AL    
	  JMP NEXT   
	  MOV AH,4CH
	  INT 21H                 
CODE ENDS
	END START